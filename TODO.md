- move more code from helpers to external packages and re-build

- Once everything is ready, create a README.md and documentation
- Create the minimal pipeline which re-creates the paper figures,
  (e.g. reactome) but still allow to run the additional analyses
  optionally
